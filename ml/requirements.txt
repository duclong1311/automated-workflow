# ML Training Requirements
torch>=2.0.0
transformers>=4.30.0
tokenizers>=0.13.0
sentencepiece>=0.1.99
# PhoBERT uses vncorenlp, but for simplicity we'll use the transformers version
# vncorenlp

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Training
tqdm>=4.65.0
accelerate>=0.20.0

# Optional: for better performance
# ninja
# flash-attn
